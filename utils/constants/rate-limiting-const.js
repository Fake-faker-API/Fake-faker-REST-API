const TIME_FRAME_IN_S = 10;
const TIME_FRAME_IN_MS = TIME_FRAME_IN_S * 1000;
const MS_TO_S = 1 / 1000;
const RPS_LIMIT = 2;

module.exports = {
  TIME_FRAME_IN_S,
  TIME_FRAME_IN_MS,
  MS_TO_S,
  RPS_LIMIT
}